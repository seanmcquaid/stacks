describe('createApiClient', () => {
  it('Returns the unvalidated data if no validation schema is provided', () => {});
  describe('Validation schema is provided', () => {
    it('Throws an error if the returned data does not match the validation schema', () => {});
    it('Returns the data if it matches the validation schema', () => {});
  });
});
